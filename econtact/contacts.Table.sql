CREATE TABLE [dbo].[Table]
(
	[Id] INT NOT NULL PRIMARY KEY, 
    [name] TEXT NOT NULL, 
    [lastname] TEXT NOT NULL, 
    [telephone] TINYINT NOT NULL, 
    [celephone] TINYINT NOT NULL, 
    [address] TEXT NOT NULL, 
    [group] TEXT NOT NULL, 
    [birthdate] DATETIME NOT NULL
)
